// Generated by CoffeeScript 1.6.3
(function() {
  var MAX_ROOM_SIZE, VALID_UPDATE_STATUSES, app, express, generateId, logfmt, normalizeURL, onPeerConnected, onPeerDisconnected, port, urlIdToPeerIds, urlIdToURL, urlToURLIds, uuid;

  express = require("express");

  logfmt = require("logfmt");

  uuid = require('node-uuid');

  app = express();

  app.use(logfmt.requestLogger());

  app.use(express.bodyParser());

  normalizeURL = function(urlRaw) {
    var urlNormal;
    urlNormal = urlRaw;
    return urlNormal.replace(/([\?#].*$)/gi, "");
  };

  generateId = function() {
    return uuid.v1();
  };

  MAX_ROOM_SIZE = 5;

  urlToURLIds = {};

  urlIdToURL = {};

  urlIdToPeerIds = {};

  onPeerConnected = function(urlRaw) {
    var peerId, peerIds, urlId, urlNormal, _i, _len, _ref;
    urlNormal = normalizeURL(urlRaw);
    if (!urlToURLIds.hasOwnProperty(urlNormal)) {
      urlId = generateId();
      urlIdToURL[urlId] = urlNormal;
      urlToURLIds[urlNormal] = [urlId];
    }
    peerId = generateId();
    _ref = urlToURLIds[urlNormal];
    for (_i = 0, _len = _ref.length; _i < _len; _i++) {
      urlId = _ref[_i];
      if (!urlIdToPeerIds.hasOwnProperty(urlId)) {
        urlIdToPeerIds[urlId] = [peerId];
        return {
          peer_id: peerId,
          url_id: urlId,
          peers: []
        };
      }
      if (urlIdToPeerIds[urlId].length < MAX_ROOM_SIZE) {
        peerIds = urlIdToPeerIds[urlId].slice(0);
        urlIdToPeerIds[urlId].push(peerId);
        return {
          peer_id: peerId,
          url_id: urlId,
          peers: peerIds
        };
      }
    }
    urlId = generateId();
    urlIdToURL[urlId] = urlNormal;
    urlToURLIds[urlNormal].push(urlId);
    urlIdToPeerIds[urlId] = [peerId];
    return {
      peer_id: peerId,
      url_id: urlId,
      peers: []
    };
  };

  onPeerDisconnected = function(peerId, urlId) {
    var index, url;
    if (!urlIdToPeerIds.hasOwnProperty(urlId)) {
      return {
        success: false,
        message: "That url_id was not recognized"
      };
    }
    index = urlIdToPeerIds[urlId].indexOf(peerId);
    if (index === -1) {
      return {
        success: false,
        message: "That peer_id, url_id pair was not recognized"
      };
    }
    urlIdToPeerIds[urlId].splice(index, 1);
    if (urlIdToPeerIds[urlId].length === 0) {
      delete urlIdToPeerIds[urlId];
      url = urlIdToURL[urlId];
      delete urlIdToURL[urlId];
      index = urlToURLIds[url].indexOf(urlId);
      if (index === -1) {
        return;
      }
      urlToURLIds[url].splice(index, 1);
      if (urlToURLIds[url].length === 0) {
        delete urlToURLIds[url];
      }
    }
    return {
      success: true
    };
  };

  VALID_UPDATE_STATUSES = {
    "DEAD": onPeerDisconnected
  };

  app.get('/', function(req, res) {
    return res.sendfile('dashboard/index.html');
  });

  app.get('/bookmarklet/:file', function(req, res) {
    return res.sendfile('bookmarklet/' + req.params.file);
  });

  app.get('/bookmarklet/compiled/:file', function(req, res) {
    return res.sendfile('bookmarklet/compiled/' + req.params.file);
  });

  app.post('/new_peer', function(req, res) {
    var result;
    if (!req.body.hasOwnProperty("full_url")) {
      res.send(500, {
        error: "Must specify full_url parameter"
      });
      return;
    }
    result = onPeerConnected(req.body.full_url);
    console.log(JSON.stringify(urlToURLIds, null, 4));
    console.log(JSON.stringify(urlIdToURL, null, 4));
    console.log(JSON.stringify(urlIdToPeerIds, null, 4));
    return res.send(JSON.stringify(result));
  });

  app.post('/delete_peer', function(req, res) {
    var result;
    if (!(req.body.hasOwnProperty("peer_id") && req.body.hasOwnProperty("url_id"))) {
      res.send(500, {
        error: "Must specify peer_id and url_id parameters"
      });
      return;
    }
    result = onPeerDisconnected(req.body.peer_id, req.body.url_id);
    console.log(JSON.stringify(urlToURLIds, null, 4));
    console.log(JSON.stringify(urlIdToURL, null, 4));
    console.log(JSON.stringify(urlIdToPeerIds, null, 4));
    if (result.success) {
      return res.send(200);
    } else {
      return res.send(500, {
        error: result.message
      });
    }
  });

  app.post('/update_peer', function(req, res) {
    var result;
    if (!(req.body.hasOwnProperty("peer_id") && req.body.hasOwnProperty("url_id") && req.body.hasOwnProperty("status"))) {
      res.send(500, {
        error: "Must specify peer_id, url_id, and status parameters"
      });
      return;
    }
    if (!VALID_UPDATE_STATUSES.hasOwnProperty(req.body.status)) {
      res.send(500, {
        error: req.body.status + " is not a valid status"
      });
      return;
    }
    result = VALID_UPDATE_STATUSES[req.body.status](req.body.peer_id, req.body.url_id);
    console.log(JSON.stringify(urlToURLIds, null, 4));
    console.log(JSON.stringify(urlIdToURL, null, 4));
    console.log(JSON.stringify(urlIdToPeerIds, null, 4));
    if (result.success) {
      return res.send(200);
    } else {
      return res.send(500, {
        error: result.message
      });
    }
  });

  port = process.env.PORT || 5000;

  app.listen(port, function() {
    return console.log("Listening on " + port);
  });

}).call(this);
